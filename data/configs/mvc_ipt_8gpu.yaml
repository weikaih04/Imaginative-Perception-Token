# Multi-View Counting - Latent 64x64 (8 GPU)
# Dataset: multi_view_counting (same dataset, default output size)
# Output image: 1024x1024 -> 64x64 latent grid (4096 tokens) - BASELINE
# Total samples: 17079

unified_edit:
  dataset_names:
  - multi_view_counting
  image_transform_args:
    image_stride: 16
    max_image_size: 1024
    min_image_size: 512
  vit_image_transform_args:
    image_stride: 14
    max_image_size: 518
    min_image_size: 224
  is_mandatory: True
  num_used_data:
  - 17079
  weight: 1
