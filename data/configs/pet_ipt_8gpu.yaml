# Perspective Taking Training Configuration - Latent 64x64 (8 GPU)
# Dataset: perspective_with_sysprompt
# Total samples: 20531
# Output image: 1024x1024 -> 64x64 latent grid (4096 tokens) - BASELINE

unified_edit:
  dataset_names:
  - perspective_with_sysprompt
  image_transform_args:
    image_stride: 16
    max_image_size: 1024
    min_image_size: 512
  vit_image_transform_args:
    image_stride: 14
    max_image_size: 518
    min_image_size: 224
  is_mandatory: True
  num_used_data:
  - 20531
  weight: 1
